#pragma once


enum LVL{
    GAME, STATUS, ERROR
};

enum OUTPUT{
    CMD, FILEOUT
};

enum MOVES{
    LEFT,
    RIGHT,
    UP,
    DOWN,
    NOWHERE,
    SAVE,
    LOAD,
    EXIT
};

enum DIF{
    LIGHT = 9,
    HARD = 11
};
